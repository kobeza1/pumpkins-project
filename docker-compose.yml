version: "3"

services:
    db:
        image: mongo
        restart: always
        env_file:
            - .env
        volumes:
            - "./data:/data/db"
        ports:
            - "27017:27017"

    server:
        build:
            context: ./server
        env_file:
            - .env
        ports:
            - "3000:3000"
        environment:
            - MONGO_URI=mongodb://db:27017/mydb
        depends_on:
            - db

    client:
        build:
            context: ./client
        ports:
            - "8888:8888"
        depends_on:
            - backend
